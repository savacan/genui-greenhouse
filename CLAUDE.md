# genui-greenhouse

Generative UI を「**何に・どう使うと効くか**」を、動くものを作りながら掴む個人実験リポジトリ（苗床）。
**目的・方針・道具立て・ロードマップは `README.md` が正。着手前に必読。**

## 目的（取り違えない）

- 主眼は **ユースケースと使い心地の探索**。「GenUI はどんなタスクに効いて、どう組むと気持ちよく、どこで破綻するか」を体感で掴む。
- **ライブラリ / プロバイダーの比較は目的ではない**。道具選びと、後の乗り換え地図として背景に置くだけ。

## このリポジトリで守ること

- **縦スライス優先・共通基盤を先に作らない**。1実験をエンドツーエンドで単独完結。重複は2回まで許す。**3回書いてる**と気づいてから `packages/` に抽出。「各層を差し替え可能にする統一インターフェース」は検証する仮説であって、最初に建てる足場ではない。`packages/` は当面空でよい。
- **オーケストレーションは段階的に**。まず単発（入力 → LLM がアクション選択 → サーバが実データ取得＆計算 → spec → 描画）。動いてから 多ターン → 操作フィードバック → ストリーミング と足す。一気に作らない。
- **計算は spec に持たせずサーバで値にして返す**（テンプレートに算術＝集計・割合・比較を入れると破綻する。json-render の `$math` も使わない）。表示整形（`$format`：通貨・％・日付）は spec 側でよい。データには出所・粒度を背負わせる。
- **1実験1ディレクトリで隔離**（`experiments/<name>/`）。各実験は own `package.json` / `.env.local` / 別ポートで独立起動。
- **LLM はプロバイダー非依存**。Vercel **AI SDK Core** 越しに叩き、プロバイダーは env で差し替え。1社にハードコードしない。json-render の標準経路は `streamText` + `catalog.prompt()`（モデルが JSONL patch で spec を構成 → 逐次描画）。構造化制約をかけたいときだけ `catalog.zodSchema()` / `jsonSchema()`。※ `generateObject` / `streamObject` は AI SDK v6 で非推奨（→ `streamText` / `generateText`）。非推奨の AI SDK 3.0 RSC（`render` API）とも別物。
- 秘密情報（API キー等）は `.env.local` のみ。コード・コミット・ログに含めない。

## スタック / ツール

- TypeScript 端から端まで（Next.js）。レンダラは json-render。LLM は AI SDK Core で抽象化。
- pnpm workspace（`experiments/*` と `packages/*`）。`pnpm --filter <name> dev` で1実験だけ起動。
- Turborepo は実験が増えてから。最初は不要。

## ステータス

- 2026-06-22: 方針を「ライブラリ比較」→「ユースケース探索＋作りながら学ぶ」に据え直し（README / CLAUDE.md 改訂）。スタックは TS / Next.js / json-render + AI SDK Core 抽象で確定。題材・使い方の型・デフォルトプロバイダーは未確定（探索中）。コードはまだ無い。
- 2026-06-23: 最初の実験を確定 = **探索ダッシュボード × 宇宙コックピット**。デフォルト LLM = Azure OpenAI（先行デモと同じ、`@ai-sdk/azure` で抽象の中。public 時は env 差し替え）。「何を作るか」の言語化を [`docs/space-cockpit.md`](docs/space-cockpit.md) に記録。コードはまだ無い。
- 2026-06-23: 一次ソース recon 完了、**Phase 0 設計を確定**。spec は LLM が構成（`streamText` + `catalog.prompt()`）／データはサーバ先取得で `initialState` 注入・LLM には生データを通さず `$state` パス＋要約のみ／計算はサーバ・表示整形のみ `$format`。json-render 実態の訂正＝駆動は `streamText`（`generateObject` ではない）・最新 **0.19.0**・peer 床 React 19 / Next 16 / zod 4。NASA(APOD/NeoWs)・ISS の実レスポンス検証済み（→ space-cockpit.md「API 確認結果」）。実装はこのセッション以降で進めてよい。
- 2026-06-25: **exp01 space-cockpit 完了・マージ済（PR #1）**。探索ダッシュボード × 宇宙の縦スライスを通し、単発オーケストレーション＋使い心地（待ちのシネマ化・Verdict-Tempo 等）を確立。
- 2026-06-25: **exp02 aftershock 完了（PR #2・`exp/02-aftershock`）**。真の agentic ループ（multi-step tool loop）× データファイアウォールの**境界地図**を `docs/aftershock.md` §8–§12 に記録。貫く軸＝**「スカラーは通す・生配列は通さない」**（ループ内・spec 経路・濃さの上限・ターン境界の全てで同型に効く）／動かすのは常に「線の幅 ↔ 射程・コスト・レイテンシ」。主な発見: 線を適切に広げると多エンティティ調査は性能・コスト両取り（§8）／compose をループに畳む案B は spec バインドが壊れ describe 層は load-bearing（§9）／多ターンは firewall をターン境界でも保つ（§10）／「濃すぎ」の上限はトークンでなく遅延＋情報過多（§11）／越境スカラーで再フェッチ税を消せる（§12）。Phase 1 マルチターン・Phase 2「別の型/題材でもう1本」を充足。
- 2026-06-26: **exp03 pokefinder 着手（`exp/03-pokefinder`）= GenUI の役割反転（出力描画 → 双方向の入力UIを組む）**。動かす唯一の変数＝「LLM が `$bindState` で two-way 入力フォームを組めるか」。設計は [`docs/pokefinder.md`](docs/pokefinder.md) が正（カクテル廃案 → PokéAPI ピボットの経緯含む）。**Phase A 完了**（データ層 `lib/finder` ＋ probe で積集合チェーンを実 PokéAPI 検証・§8）。**Phase B 完了**（レンダラ写経＋two-way 部品＋ハンド spec を :3103 でブラウザ検証・§9）。**Phase C 完了**（単発 compose 2モード=問い→フォーム生成 / 探す→サーバ findMons→結果ボード を Azure OpenAI 実呼び出し＋Playwright で全ループ検証・§10）。**核の答え＝LLM は two-way 入力フォームを正しく組める**（$bindState パス一貫・spec.state 初期選択・関係タイプだけ出す判断・exp02 §9 の構文発明は出ない）。いちばんの落とし穴は LLM でなく**クライアント配線**＝json-render の `ActionProvider` が handlers を `useState` で初回マウント時に凍結＋FinderRenderer が Compose-Live で streaming 中マウント → `streaming=true` 版 onFind が凍結され「探す」無反応。修正＝handler 安定参照（`useCallback([])`）＋可変値は ref 越し。データ形の知見: `type/<t>` のフォーム重複が UX を埋める→世代フィルタ（species 名のみ）が自動 de-dup（Phase A #1 のライブ実証）。要点: store は `initialState` から seed（`spec.state` でない＝spec.state を抽出して渡す）／`$template` は core binding 式・トークン `${/ptr}`。**組成品質の多サンプル評定（§11）も実施**: 実モデル 12 クエリ収集 → ワークフロー多レンズ判定（fitAvg/intentAvg 4.45/5・機械的契約は全12完璧）。最大の発見＝**OR→AND のサイレント意図反転**（「炎か飛行」を両 pre-check→サーバ AND で「炎かつ飛行」に潰れ意図反転）。境界は「非表現語（OR・範囲・情緒）を fabricate しないか」でなく「**黙って近似するか/明示・中立化するか**」＝入力役割固有の新破綻面。**form 永続 live 再検索も実装（§12）**: §9 の remount-flash を **controlled StateStore（`createStateStore`＋`JSONUIProvider store=`＝initialState/onStateChange 無視）の単一永続ボード**で正面突破。LLM が form＋結果リージョンを1枚に compose、「探す」は LLM 不使用で `/api/find`（計算のみ）→`store.set("/findMons",…)`→MonGrid live 更新。実機で「トグル→探す→結果 in-place 更新＋選択全保持（flash なし）」を確認。トレードオフ＝**「LLM 再 compose の表現力」↔「状態保持＋低レイテンシの双方向 FEEL」**（GenUI に state を持たせると立つ新軸・controlled store は保持側の正攻法）。注意: controlled store は spec.state を自動 seed しないので `store.set("/shelf",…)` で手動 seed。**非表現語の graceful 明示も実装＋再測（§13・発見→修正→再測の閉ループ）**: compose プロンプトに「サイレント縮約禁止＝表現不能語は中立(false/null)＋Text 明示」を追加。実機 curl で確認＝炎か飛行は fire のみ ON＋「『か』はAND条件で表現不能…」明示／第5世代以降は gen null＋「範囲指定できません」明示／かわいいは fairy 確定せず＋明示。OR(片側ON)と「両方」(AND)を正しく区別。再測 intentAvg 4.45→4.58↑（かわいい int2→4）・fitAvg 4.45→4.33↓。**2つの教訓**: (1) 測定ハーネス（収集）が説明 Text を捨てていて判定が「告知付き縮約 vs 無告知」を切り分けられず防御的減点＝**re-measure は修正が変える要素を観測できる粒度で作る**(notes 捕捉を追加して是正)。(2) OR/範囲は**サーバが原理的に表現不能**＝プロンプトは graceful 劣化止まり・忠実化はサーバ拡張(findMons に OR/世代範囲)が要る＝exp02「線の幅↔射程」と同型(線=サーバのクエリ言語の表現力)。**PR #3 作成済**（`exp/03-pokefinder` → main・未マージ）。**指差しで組み直す＝出力ジェスチャ → 入力UI 再合成も実装（§14）**: 結果カード（出力 MonGrid）に入力アフォーダンス「◎ これに似た相棒を」を乗せ、クリックしたモンを `seedMon`（client 既知の値・再フェッチなし＝exp02 越境スカラー）に LLM がフォームを再 compose。配線は §12 と同型＋ `AnchorContext`（registry 外 React context・動的データ行 `/findMons/mons` には per-element の static monId を spec emit に載せられない＝json-render 構造制約の再確認）→ `onAnchor`(安定 useCallback＋ref)→`sendMessage({body:{seedMon}})`→route `parseSeedMon`→`buildFormPrompt` seed 分岐。実機＋型数違い4体で検証＝全件 graceful（2タイプ起点は中心1タイプのみ ON＋OR 明示・§10 凍結再発なし・§12 保持を壊さない）。**正直な結論**: (1) 新規は「役割の向き＋非テキスト入力モダリティ」で安く一段深い反転、(2) graceful OR は **§13 規律のロバスト性**であって LLM 自律判断ではない（seedSection が「全 ON するな」と口述）、(3) 鏡像の未検証破綻＝「似た＝OR」はサーバ作者の一方向決め打ちで AND 本意ユーザーには「AND→単一タイプ」の新サイレント縮約を作りうる。忠実化は §13 同様サーバ拡張（findMons の OR/世代範囲）＝線の幅↔射程に帰着。レビューで露出した trust-boundary バグ（不正 seedMon の無言クラッシュ）は `parseSeedMon` で修正済。§14 のコミットは PR #3 に乗る。**線を太くして忠実化も実装（§14b）**: §13/§14 が指した「OR/範囲はサーバが原理的に表現不能」を、findMons に `typeMode: and|or`（or=和集合）＋世代範囲 `genFrom`/`genTo` を足して**実際に線を太く**（exp02「線の幅＝サーバのクエリ言語」を太らせた）。LLM は typeMode Select・世代(から/まで)2 Select を compose・buildFormPrompt/seedSection を「OR は typeMode=or で忠実・範囲は genFrom/genTo・主観語だけ依然中立＋明示／似た=OR は両読み明示」に改訂。実機検証＝「炎か飛行」→typeMode=OR・探すで single-fire/single-flying 両方入り和集合実証＝**OR→AND サイレント縮約が源から消えた**。8サンプルで `overAppliedOr=false`（OR を過剰適用せず使い分け）。§14 鏡像（似た=OR 一方向決め打ち）も両読みで解消。**正直な核（途中で実バグ・ユーザーが手で触って発覚→訂正）**: (1) **致命バグ＝「強い順」が壊れていた**。findMons は候補名を**アルファベット順 sort → 上位60件だけ**種族値 fetch してから total 並べ替えていた（`cand.sort()`→`slice(0,60)`）。候補>60（fire=109・water=192・OR=255+）だと真の最強が前綴り外に落ちる＝「最強の炎」で reshiram(680)、OR fire∪flying で rayquaza(780) が消える＝**中心ユースケースで誤答**。(2) **「開示」は「正解」の代わりにならない**: 初版レビューで私はこれを「線を広げた代償・truncation note で明示するトレードオフ」と**誤って正当化**した（開示を live に繋ぐ修正もした）が、**間違った答えに注釈を付けても正解にならない**。正しい修正＝**全候補を fetch して母集団全体でランク**（MAX_DETAIL を 600 安全弁に・現実クエリ最大~450 は全 fetch・truncated=false）。修正後 reshiram/rayquaza が正しく上位（probe ⑫ assertion＋live /api/find で確認）。コストはレイテンシ（255件全 fetch でも ~5s）＝exp02 線の幅↔レイテンシの正直な支払い。(3) **検証の穴（最大教訓）**: 自動チェック（件数・機構・curl 契約・開示）は通っていたが**「答えが正しいか（ランキングが真の最強か）」を検証していなかった**→correctness バグが ship 一歩手前。**機構が動く・正直に見せる ≠ 結果が正しい**。probe に「真の全件ランクと突き合わせ」assertion(⑫)を恒久追加。(4) §13 閉ループ再帰＝eval-collect の badPath whitelist stale で新パス誤判定→更新。§14b コミットも PR #3 に乗る。**がっつり監査で違和感の正体を特定＋総点検も実施（§15・8サーフェスを実 PokéAPI と数値突き合わせ）**: ユーザーが手で触り「ロジック壊れてる気がする・違和感すごい」。監査でランキング数式・stat マッピング・集合演算・配線は ground truth と完全一致＝**計算は健全**と確認。**違和感の正体＝フォーム汚染（データ設計の問題・計算バグでない）**＝`type/<t>` が別形態（メガ/キョダイ/primal/origin/ultra/eternamax、id≥10000）を全部含み de-dup せず total 順で並べるため異形がボード上段を独占（dragon 1位 eternatus-eternamax 1125・top24 の15/24 が別形態）。隠れた実 major バグ＝世代フィルタが form 名と species 名を生交差し giratina/zygarde/tatsugiri を誤消滅。従因＝sortBy 未配線で常に total 順（「素早さ高め」が効かない）。**修正**: (a) フォーム正規化＝既定 `is_default` の base 種のみ＋LLM が `Toggle`「別形態も含める」(/shelf/includeForms 既定 false)を compose、(b) 世代フィルタを `species.name` で（giratina 生存＋gen 有無の挙動一致・正規化と同経路で同時解消）、(c) `sortBy` 配線＝LLM が `並べ替え` Select を compose し問いの強調軸を初期値に、(d) 軽微（params→400・types 3 cap・世代ラベル min/max＋開放端・Number 正規化）、(e) デッドコード削除（idFromUrl・generate の intent=find 分岐・buildResultsPrompt→§12 単一 live 経路に統一）。**検証（ground truth・恒久化）**: probe ⑫ を base/forms 別の真ランク突き合わせに拡張＝両 PASS。実機 dragon 既定→eternamax/メガ消滅・実在の相棒／gen4→giratina 生存／includeForms ON→別形態復活／sortBy=speed→dragapult 1位／「素早さが高いドラゴン」を LLM が組む→speed Select＋下限110＋トグル OFF→速い実在ドラゴンが速度順（別形態0）。**最大の教訓（§15）**: 計算を何度検証しても「出てきた“もの”が妥当か」を見ないと破綻は出ない＝eternamax 1位は Phase A からずっとで機構/件数/stat 検証は全通過・**人が触るまで誰も見なかった**。「違和感」は一級の信号。破綻は計算でなく**データの粒度（フォーム名 vs 種名）という設計判断**＝exp02「線の幅↔射程」の線は母集団の粒度でもある／実 API は人間の概念より細かいので**ドメインの正しい粒度への正規化責務はサーバに残る**。[[verify-output-correctness-not-just-mechanics]]。§15 コミットも PR #3 に乗る。**エンドツーエンド意味検証も実施（§16・§15 の教訓を反復可能な測定に）**: クエリ→LLM compose→`toFindParams`(lib/finder/shelf.ts に切り出し page と eval で共有)→`/api/find`→返ってきたポケモン、を14クエリ通し「相棒が問い通りか」を実 PokéAPI で多レンズ判定（eval-e2e.mts 収集→ワークフロー14判定）。**結果 scoreAvg 3.79・14中9完全忠実・核「LLM は双方向フォームを組める」は YES 確定（OR→AND 反転も克服）**＝残る破綻は全てサーバ側。**この検証が初めて出した破綻（§15 監査が見逃した）**: (1)🔴blocker＝1件の 404 で検索全体クラッシュ（type union の幽霊フォーム名 meowstic-male-mega が /pokemon で 404→mapLimit の Promise.all reject→/api/find 死亡・gholdengo 等7件消失。§15 監査の「404ゼロ」はサンプリングの穴）→候補 fetch を per-item try/catch で 404→null 除外。(2)🟠false-disclosure＝types.min(1) 必須をフォームが知らず「タイプ未指定でも探せる」と嘘→prompt に types 必須明記＋「種族値だけでは探せない・タイプ選んで」。(3)🟠能力述語の偽装＝「メガできる」を includeForms にすり替え（includeForms は別形態を“表示”であって“絞る”でない）→中立化＋「絞れません」明示・proxy 禁止。(4)data＝PokéAPI に非公式 fan-made メガ（heatran-mega 等が 200）＝「PokéAPI=公式」前提崩れ・docs 記録。**修正＋同14再測**: 404→gholdengo 復活／「壁」→steel probopass（検索なし解消）／「メガできる」→includeForms=false・明示／「タフ」0→1件／既存9無回帰。エラー経路の内部 URL 漏れも中立文化・0件 note も原因名指しに。**§16 結論**: 「出力の妥当性を見る」は反復可能な測定にでき、構造化監査が見逃した blocker を一発で出す。入力役割（フォーム compose）の品質は load-bearing に高い／「毎回確実に」を阻むのは一貫してサーバ側（表現力・堅牢性・データソース）＝exp02 線の幅↔射程と同型・GenUI の伸びしろは LLM でなくサーバの射程と堅牢性。§16 コミットも PR #3 に乗る。**NEXT 候補**: PR #3 マージ／findMons に is_legendary 等の射程拡張／別題材で役割反転の別面。
- 2026-06-29: **exp04 artfinder 完了（`exp/04-artfinder`・PR 予定）= 役割反転を別題材でもう1本**（題材 = Art Institute of Chicago）。設計は [`docs/artfinder.md`](docs/artfinder.md) が正。pokefinder と同じ finder アーキタイプ・同じ入力役割のまま題材を AIC に替え、**§16 の結論「伸びしろはサーバ側」が別ドメイン・別クエリ形でも立つか**を検証。pokefinder と違い**上流(AIC)が豊かな ES クエリ DSL を持つ**ので「線の幅」は自前 findMons でなく**サーバの form→ES 翻訳忠実度＋上流の射程**に移る。**Phase A**（probe 9チェック全 PASS・出力 correctness ⑧ まで・form→ES 翻訳＝ファセット内 OR/間 AND・年代/色相 range・常時 exists:image_id）**Phase B**（新規 two-way 部品 **ColorSwatch/TextInput/RangeSelect/FacetCheckbox** をブラウザ検証・トグル→探す→ live in-place＋選択保持）**Phase C**（実 LLM compose・「青っぽい近代の油彩」→絵画+hue215+1900〜＋明示／「有名な彫刻」→主観語中立化＋明示）。**E2E 多レンズ判定（13クエリ・発見→修正→再測の閉ループ）= fidelityAvg 4.5・fitAvg 3.9・忠実 12/13・核は YES**（LLM は別ドメインでも two-way フォームを正しく組める・**OR→AND サイレント反転は再発せず**＝pokefinder 規律の移植成功・新モダリティの色相/テキスト/レンジも two-way 動作）。**破綻は一貫して server/data 側**（locus llm 3・server 2・data 2＝クエリ言語の表現力＝版画の地域/主題/価格/OR・データ粒度＝版画は別部門・date_start=0 欠損・medium サブファセット不在）。**閉ループの教訓＝プロンプト nudge は単調改善でない**（狙った3破綻=版画×欧州0件/赤い抽象画の絵画型取りこぼし/「古い」yearTo=0 過剰 を直すと **#10「高価な絵画」が回帰**＝「高価」中立化が表現可能な「絵画」型まで巻き添えに＝「表現不能語の中立化」と「表現可能語の符号化」の境界に run 変動）。**実装の核知見**: (1) 自由語は AIC top-level `q`（並べ替えだけ）でなく `multi_match` の must で**実際に絞る**（件数を正直に・§16）。(2) 種別ファセットは `classification_title`（"oil on canvas" 等技法混じり細粒度）でなく `artwork_type_title`（"Painting" 粗粒度・§15「粒度はサーバ」）。(3) `term` はテキスト体に **`.keyword` 必須**＝H3「翻訳器の fiddliness はサーバが背負う」。**🔴 データ源クセ（§16級・実ブラウザ検証で初めて判明・curl では見えない）= AIC 画像は cross-origin 表示不可**（画像ホスト www.artic.edu/iiif が Cloudflare JS challenge＝サーバ fetch も 403で画像プロキシ不可・かつ CORP `same-origin` でブラウザ cross-origin `<img>` が `ERR_BLOCKED_BY_RESPONSE.NotSameOrigin`）→第三者/localhost アプリで写真を出せない＝各作品の `color`(HSL) で**色チップ視覚化**に転化（「青い絵画」は青の色面＝色フィルタの視覚的証明）。配線 gotcha（§10 handler 凍結・§9 remount-flash）は写経した controlled store＋安定 handler で**再発せず**。exp02「線の幅↔射程」/ §15「粒度はサーバ」が別ドメインで再確認＝GenUI の伸びしろは LLM でなく**サーバの射程と母集団の粒度**。[[verify-output-correctness-not-just-mechanics]]。**NEXT 候補**: PR 作成・マージ／findArt の射程拡張（主題タグ subject・地域メタ正規化・クロスファセット OR）／画像代替表示の検討／さらに別の役割（編集・構成）への反転。
- 2026-06-30: **exp04 §13＝実際に線を太くして fit を測る（`exp/04-artfinder`・PR #4 に追加）**。§12 が「破綻は server/data 側＝線を太くすれば fit は上がるはず」で止めた未測定点を測る。着手前 probe で**過去ログの“限界”2つが実は findArt の翻訳ギャップ**と判明（クロスファセット OR は ES の `should+minimum_should_match` で表現可・medium も `classification_titles.keyword` で到達可＝上流の限界でなく線が細く引かれていた・[[verify-output-correctness-not-just-mechanics]] の逆＝過去ログも疑う）。**足した線（全て probe で実 AIC 突き合わせ・⑩⑪⑫恒久追加）**: (1) **主題 subject**＝`match subject_titles`（水辺→water・抽象→abstract）＋q を作者/作品名に分離（term_titles 除去＝「q が主題を暗に約束」嘘を解消）、(2) **産地 region**＝`match place_of_origin`（国は直 match・**大陸語はサーバが代表国 OR へ展開**＝§15 正規化はサーバの責務）＋**版画も産地で地域を絞れる**（§4「版画×地域 0件」を開示でなく実フィルタに忠実化）、(3) **クロスファセット OR `combineMode`**（and/or）＝軸またぎ OR を内容条件の should に（pokefinder §14b の typeMode を cross-facet に持ち上げ）、(4) 候補件数の開示（上位N件表示）。compose は §4/OR の「表現不能」開示を**忠実化へ反転**（黙って AND に倒さず or・地域は region）。⑫は**包除原理が厳密成立**で「OR は近似でなく本物」を検証（`OR==A+B−AND`）。**結果（同17クエリ＝§11 の13＋新射程4・多レンズ判定）**: Round 1 で **fidelity 4.5→4.82・fitAvg 3.9→4.63・忠実 12/13→16/17・locus llm3/server2/data2→llm1/server2**（§4 版画×欧州=region+print で 5/5・#12 水辺=subject・#16 絵画か青い=combineMode or 候補11696 和集合・#17 アフリカ染織=region 大陸展開）。**閉ループ Round 2**＝#5「赤い抽象画」が type=painting 取りこぼし（唯一の faithful=false）→「『〜画』複合語は subject＋type=painting 両取り」を compose に追加→#5 fid3→5/fit3→5・**忠実 17/17・fidelity 4.88**・nudge 般化で #12 も subject="water landscape"。**だが #10「高価な絵画」が新規回帰**（5/5→3/3・「高価」中立化時に表現可能な「絵画」type を落とし skip）＝**§11 Round 2 と同じ #10 失敗を live 再現＝プロンプト nudge は非単調**（#5↔#10 whack-a-mole・#5 修正が原因でなく独立した run 変動）。**§13 の結論**: (1) **サーバの線拡張は fit を単調に上げる＝thesis を数値で実証**（3.9→4.6・忠実 17/17）＝exp02「線の幅↔射程」を議論でなく測定で／GenUI の伸びしろがサーバの射程にあるを別ドメインで定量化、(2) 過去ログの“限界”は鵜呑みにせず probe で実突き合わせ（OR/medium は翻訳ギャップだった）、(3) **残る server 側 fit 残課題＝主題の“ランキング”**（subject の集合 matchedCount は正しいが自由語無しの relevance ソートが人気順に流れ top-N が主題特異度で並ばない＝「睡蓮」で Monet の Water Lilies が先頭に来ない・**集合は正しいが並びが弱い**＝線の幅でなくランキング品質の射程・NEXT=主題 relevance ブースト/sort）、(4) **compose nudge の非単調性は LLM 側 run 変動でサーバの線拡張（ロバストな単調改善）とは別物**。検証＝probe ⑩⑪⑫ live PASS／ブラウザで combineMode compose→探す→in-place 更新＋選択保持（§9/§10 再発なし）＋カード産地表示／E2E 17×2 round。§13 コミットは PR #4 に乗る。
